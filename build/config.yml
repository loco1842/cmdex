# This file contains the configuration for this project.
version: '3'

info:
  companyName: "fenv"
  productName: "CmDex"
  productIdentifier: "com.fenv.cmdex"
  description: "CLI command manager with variable placeholders"
  copyright: "© 2026, fenv"
  comments: "CmDex - Command Manager"
  version: "0.3.0"

dev_mode:
  root_path: .
  log_level: warn
  debounce: 1000
  ignore:
    dir:
      - .git
      - node_modules
      - frontend
      - bin
    file:
      - .DS_Store
      - .gitignore
      - .gitkeep
    watched_extension:
      - "*.go"
      - "*.js"
      - "*.ts"
    git_ignore: true
  executes:
    - cmd: wails3 build DEV=true
      type: blocking
    - cmd: wails3 task common:dev:frontend
      type: background
    - cmd: wails3 task run
      type: primary
